{{!< default}}
{{#page}}
<article id="{{id}}" class="{{post_class}}">
  <h1 class="post-title">{{title}}</h1>
  
  {{content}}

  <section class="subscribe-form-section">
    {{> newsletter-form}}
  </section>
</article>
{{/page}}
